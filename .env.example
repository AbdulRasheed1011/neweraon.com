# Copy this file to .env and fill in your values
# NEVER commit .env to git

# Your AWS Lambda / API Gateway URL that proxies Claude API calls
# See README for how to deploy the Lambda function
VITE_API_URL=https://YOUR_LAMBDA_URL/chat

# (Optional) Direct Claude API key — only use for local dev, never expose in production
# VITE_CLAUDE_API_KEY=sk-ant-...
